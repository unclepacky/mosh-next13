"use client";

const AddToCard = () => {
  return (
    <div>
      <button
        className="btn btn-primary"
        onClick={() => console.log("Clicked")}
      >
        Add to card
      </button>
    </div>
  );
};

export default AddToCard;
